# Migrations

Migration files will live here after the database tool is selected. Migrations must be forward-only, reviewable, and safe for rolling deployments. Never edit an applied migration; add a new one.
