# P4.4 — Verify benefits and costs against the monolith

_Part of the [delivery roadmap](../ROADMAP.md)._

- [x] **Task 1.** Run P3.5 benchmark scripts unmodified against the extracted topology,
      changing only documented target/configuration inputs. Match workload,
      resources, cache, user/batch distribution, and total provider concurrency;
      identify unavoidable differences explicitly. (2026-09-24)
- [ ] **Task 2.** Exercise isolated scaling, discovery/provider outage, scan backlog, delayed
      confirmation, and service rollback. Test unrelated library journeys where
      dependencies permit. PostgreSQL failure remains a shared failure boundary.
- [ ] **Task 3.** Compare latency distributions, throughput, errors, confirmation delay,
      build/deploy time, operational steps, and monthly cost against P3.5's
      declared budgets. Separate provider variance and earlier CI gains.

Exit: publish reproducible before/after samples and a keep/revise/reverse decision
for each extraction. Explain and address failed budgets before calling the
topology ready. Finding that the monolith is sufficient is a valid evaluation
result; unrun demonstrations are not completed work.

## Task 1 — matched benchmark rerun (2026-09-24)

The P3.5 harness source was not changed (`git diff -- scripts/benchmark
scripts/concurrency` was empty before and after the runs). The current-code
sample is at commit `df3ea66ce308be6fcc4e4dc7bf42a8c0f4650331`; both reports
say the tree was dirty because unrelated/generated files already existed, so
the commit plus the files in this change, rather than `workingTreeDirty: false`,
is the reproducibility boundary.

Published pairs:

- HTTP ingestion baseline:
  [`2026-09-17T21-07-11-939Z`](../../scripts/benchmark/results/2026-09-17T21-07-11-939Z/summary.md);
  current scan/core split:
  [`2026-09-24T13-09-45-226Z`](../../scripts/benchmark/results/2026-09-24T13-09-45-226Z/summary.md).
- Worker/provider-concurrency baseline:
  [`2026-09-18T16-22-43-565Z`](../../scripts/concurrency/results/2026-09-18T16-22-43-565Z/summary.md);
  current scan/core split:
  [`2026-09-24T13-12-47-452Z`](../../scripts/concurrency/results/2026-09-24T13-12-47-452Z/summary.md).

### Reproduction and parity

Both dedicated databases were dropped and recreated before the final sample.
Only documented configuration inputs changed: `SCAN_DATABASE_URL` and
`CORE_DATABASE_URL` retained their documented least-privilege role credentials
but targeted the harness's `vinylhound_e2e_bench` or
`vinylhound_e2e_concurrency` database; the concurrency run set
`CONCURRENCY_MODES=stub,live`, `CONCURRENCY_WORKER_COUNTS=1,2`, and
`CONCURRENCY_STUB_SCANS=25`. All other values were the documented defaults.
The commands remained `npm run bench:load` and
`npm run concurrency:compare`.

| Dimension              | P3.5 baseline                                                                        | P4.4 Task 1                                                                                                          | Match                             |
| ---------------------- | ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- | --------------------------------- |
| Host/runtime           | Intel Core 7 150U, 12 logical CPUs, 15.7 GiB, Windows x64, Node 24.18.0              | Same machine and runtime                                                                                             | Yes                               |
| Ingestion workload     | 5 users × 2 batches × 5 scans, concurrency 10, 3 timed runs                          | Same                                                                                                                 | Yes                               |
| Ingestion warmup/cache | 2 users × 1 batch × 3 scans discarded; one long-lived web/worker process             | Same                                                                                                                 | Yes                               |
| Local resources        | One production-build web process, one synthetic worker, local PostgreSQL/Redis/MinIO | Same resources; web and worker now open separate scan/core role-scoped connections to the same PostgreSQL deployment | Yes, with the intended role split |
| Worker workload        | 25 stub and 5 live scans for each of 1 and 2 worker processes                        | Same                                                                                                                 | Yes                               |
| Provider concurrency   | Total 2: one worker at 2, or two workers at 1 each                                   | Same                                                                                                                 | Yes                               |
| Provider fixture/cache | Same generated 600×600 JPEG; one long-lived web process across all four legs         | Same                                                                                                                 | Yes                               |

Headline integrity checks, before the fuller Task 3 budget comparison:

- Ingestion completed 150/150 timed pipelines with zero errors. Across the
  three runs, throughput was 27.72–32.75 scans/s, full-pipeline p50 was
  289.60–338.65 ms, and p95 was 348.24–429.21 ms.
- Concurrency completed 50/50 stub and 10/10 live analyses with no pipeline
  error while holding total concurrency at 2. The incremental live cost was
  $0.0146 for the one-worker leg and $0.0147 for the two-worker leg ($0.0293
  total).

These figures establish the matched sample; they are not yet a topology
keep/revise/reverse decision. Task 3 owns budget adjudication and must separate
provider variance from application time.

### Differences and limitations that cannot be hidden

- P3.5's workload contains no catalog/discovery request. In addition, the
  repository's development tier intentionally keeps discovery in-process, and
  the unmodified harness starts local web/worker processes rather than accepting
  a remote staging base URL. This sample therefore exercises the scan/core
  role/schema split and worker boundary, but cannot measure P4.1's discovery
  network hop or P4.3's delivery-repository split. Task 2's staging exercises
  and Task 3's deploy/cost evidence cover those boundaries.
- Migration 022 intentionally removed cross-schema user foreign keys. The old
  harness reset (`truncate users, albums cascade`) therefore no longer clears
  scan-owned rows between concurrency legs. Fresh random users keep the timing
  samples independent, and timing records come only from each leg's worker
  stdout, but `estimatedCostUsd` now accumulates across live legs. The report's
  first live value is $0.0146 and its second is $0.0293 cumulative, so the
  second leg is $0.0147 by subtraction. Recreating the dedicated database before
  the invocation restored the same clean starting state; no production or
  development data was touched. This is a harness limitation caused by the
  intentional FK removal, not evidence of an application-cost regression.
- The live baseline report did not persist model/detail identifiers. The
  current run used the project's configured `gpt-5.6-terra`/`high` inputs, the
  same configuration family documented around the P3.5 live sample, but exact
  equality cannot be proven from the old artifact. Provider service conditions
  also changed across the six-day interval. Total concurrency, fixture, scan
  count, and client-side application path are exact matches; Task 3 must treat
  live latency changes as provider variance unless corroborated elsewhere.
- The current commit necessarily includes all changes since P3.5, not only the
  extraction commits. Same-host deterministic-stub results are the application
  comparison; live-provider improvements cannot be attributed to extraction.
